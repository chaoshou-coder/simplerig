[project]
name = "simplerig"
version = "0.1.0"
description = "多Agent工作流框架，完全可配置"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0.0", "pytest-bdd", "black", "ruff"]

[project.scripts]
simplerig = "simplerig.cli:main"
